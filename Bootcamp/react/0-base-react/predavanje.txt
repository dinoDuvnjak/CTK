1. pricaj o reactu i air bnv
2. procaj o strukturi projekat, objasni svaki fajl
3. pocni sa jsx i babel
    / stari nacin
    // var React = require('react');
    // var ReactDOM = require('react-dom/client');

    const root = ReactDOM.createRoot(document.getElementById('root'));
    root.render(myElement);
    root.render(<h1> Hello, world!</h1>); // ovo je samo jedan element, radi

4. prebaci na import standard sada  
5. prbaj dodati jos HTML jedan element i pokazati zasto to NE radi, uvijek prima samo 1


    

